# 크로스플랫폼 개발 — 저장소는 LF 로 통일한다.
* text=auto eol=lf

# 바이너리 산출물 (diff/eol 변환 금지)
*.xlsx  binary
*.pptx  binary
*.png   binary
*.jpg   binary
*.ico   binary

# SQL 스크립트는 UTF-8(BOM 없음)이며 sqlcmd -f 65001 로 적용한다.
*.sql   text eol=lf
